//wap to find a number is less or greater than other

#include<stdio.h>
int main()
{
    int a,b;
    printf("Enter your number: ");
    scanf("%d",&a);
    printf("Enter the value with which you want compare: ");
    scanf("%d",&b);

    if(a<b)
    {
        printf("%d is less than %d",a,b);
    }
    else
    {
        printf("%d is less than %d",a,b);
    }
    return 0;

}